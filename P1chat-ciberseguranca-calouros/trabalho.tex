
%\input{secao1}
\section{Nova seção}

A primeira atividade do mês de abril, foi uma pesquisa sobre:
\begin{itemize}
    \item Malware --- programas maliciosos criados para danificar ou espionar computadores.
    \item Ransomware --- tipo de vírus que ``sequestra'' os arquivos e pede dinheiro para devolvê-los.
    \item Trojan (Cavalo de Troia) --- programa que se disfarça de algo útil, mas esconde uma ameaça.
    \item Phishing --- golpe que tenta enganar a pessoa para roubar senhas ou dados (por e-mail ou site falso).
    \item DDoS --- ataque que sobrecarrega um site com tantos acessos que ele sai do ar.
    \item Man-in-the-Middle --- quando alguém intercepta a comunicação entre duas pessoas sem que elas percebam.
\end{itemize}

\subsection*{Cybersegurança: Vírus (Trojan)}

Trojan é um tipo de malware que engana o usuário sobre sua verdadeira intenção. Geralmente, é espalhado por engenharia social, e-mails ou até anúncios falsos.

A origem do nome ``trojan'' ou ``trojan horse'' (em português, ``cavalo de Troia'') vem da própria Guerra de Troia e do cavalo feito por Odisseu. Esse tipo de malware recebe esse nome porque se passa por algo legítimo, como processos de autenticação, com o objetivo de obter senhas dos usuários. Assim, com a senha e a conta, o invasor pode se passar pela vítima.

Hoje em dia, os métodos estão mais complexos: eles se disfarçam de programas legítimos. Contudo, em contraste com vírus e worms, não criam cópias de si mesmos --- esse é o motivo de não serem considerados vírus propriamente ditos. Dessa maneira, instalam-se diretamente no computador de forma silenciosa.

A popularidade desse método se deve muito à sua facilidade de uso, pois qualquer pessoa pode acessar o computador de outra apenas com um arquivo. Eles também podem ter programações para realizar autodestruição após um comando ou determinado tempo.

As infecções podem ocorrer, normalmente, de três maneiras: Engenharia social: o usuário recebe um e-mail infectado ou sofre um ataque de phishing; ao clicar ou baixar o arquivo, acaba sendo infectado. Downloads maliciosos: geralmente por sites não confiáveis ou softwares piratas; ao baixar esses programas, a máquina passa a ser infectada. Vulnerabilidades de segurança: menos comuns, ocorrem quando o usuário não baixa nada, mas possui um sistema desatualizado ou antigo, tornando-se alvo de ataques.

Geralmente, após a instalação, o trojan possui três objetivos principais: Roubar dados da vítima, como informações bancárias, senhas ou contas; Controlar remotamente o dispositivo, podendo utilizá-lo em outros ataques; Instalar outros malwares.

Na maioria dos casos, é difícil perceber se você foi vítima ou não. Contudo, há alguns sinais, como: baixo desempenho da máquina (travamentos recorrentes ou até a ``tela azul''); alterações na área de trabalho e barra de tarefas (resolução, cor, ícones diferentes); presença de programas desconhecidos no gerenciador de tarefas; aumento de e-mails spam, anúncios e redirecionamentos para sites suspeitos.

Para evitar esses problemas, é recomendável: bloquear certos tipos de anexos em e-mails (como *.EXE, *.BAT, *.CMD, *.SCR e *.JS), evitar programas suspeitos, utilizar antivírus, usar VPNs e manter aplicativos e softwares sempre atualizados.

Alguns dos principais tipos:
\begin{itemize}
    \item Backdoor: o atacante invade o computador infectado e pode fazer praticamente qualquer coisa, controlando-o remotamente. Pode enviar, receber, executar e apagar arquivos, além de reiniciar o sistema. É frequentemente usado para criar redes zumbi (botnets).
    \item Exploits: tiram proveito de vulnerabilidades em softwares ou aplicativos do computador infectado.
    \item Clampi: aguarda uma transação bancária para roubar dados da conta. É extremamente eficiente em se ocultar e burlar firewalls.
    \item Cryxos: a vítima recebe um aviso falso de infecção com um número de telefone; ao ligar, é chantageada por dinheiro ou induzida a conceder acesso à máquina.
    \item FakeAV: fingem ser antivírus. Assim como os cryxos, tentam extorquir dinheiro com ameaças falsas.
    \item IM: acessam logins de aplicativos de mensagens, como WhatsApp, podendo ser usados para extorsão, envio de malware ou até ataques DDoS.
    \item DDoS: realizam ataques distribuídos de negação de serviço, sobrecarregando um site com múltiplas solicitações, tornando-o indisponível.
    \item Mailfinder: roubam senhas de e-mail para enviar vírus aos contatos da vítima.
    \item Ransom: tornam o computador inutilizável e só restauram o acesso mediante pagamento, sendo usados em extorsões.
    \item Acesso remoto (RAT): permitem ao invasor controle total do dispositivo, possibilitando roubo de dados, espionagem e distribuição de outros malwares para formar botnets.
\end{itemize}

Referências:
\begin{itemize}
    \item \url{https://www.eset.com/br/blog/cultura/o-que-e-um-trojan-como-funciona-e-como-se-proteger/}
    \item \url{https://pt.wikipedia.org/wiki/Cavalo_de_troia_(computação)}
    \item \url{https://www.kaspersky.com.br/resource-center/threats/trojans}
\end{itemize}

A segunda pesquisa do mês de abril foi:
Entender o básico de como a internet funciona:
\begin{itemize}
    \item Como dados viajam de um computador a outro de forma simplificada.
    \item O que é um endereço IP (o ``endereço'' do computador na rede).
    \item O que é uma porta (como ``portas de entrada'' para diferentes serviços).
    \item O que é cliente-servidor (um computador pede, o outro responde).
\end{itemize}

\subsection*{Migração de Dados}

A viagem de dados ocorre por meio de redes físicas (cabos) ou sem fio (wi-fi), possuindo diversas maneiras de se transferir, convertendo informações de uma máquina para outra em sinais elétricos, luz ou rádio, sendo transmitidos e recebidos em pacotes. Esses pacotes podem seguir caminhos diferentes pela internet para evitar congestionamentos e, ao chegar ao destino, são organizados novamente na ordem correta. Isso pode ser feito de forma que se conecte a mais do que dois computadores, podendo conectar cada um em um certo computador, porém é uma forma muito complicada, por isso é utilizado um roteador que pode conectar a outros roteadores para transmitir esses dados de uma forma que necessite menos cabos, podendo se escalar a uma fonte infinita de computadores, sendo uma forma mais facilitada o uso de um provedor de serviços de internet (ISP) feito especificamente para isso, facilitando ainda mais a conexão.

\subsection*{IPs (Internet Protocol)}

O endereço IP é usado para identificar um computador para a recepção ou o envio de dados de um dispositivo específico, possuindo geralmente um formato composto em série de 4 números separados por pontos, por exemplo: 192.123.5.29. Sendo uma forma eficiente para computadores, porém para humanos não possui essa vantagem, por isso IPs podem ser apelidados, conhecidos como nome de domínio, por exemplo google.com, facilitando a sua compreensão, assim como ajuda na sua localização visto que endereços IPs mudam toda hora. IPs também possuem diferenças quanto ao IP público e privado, sendo o público o endereço do roteador na internet, já o privado é o endereço da sua própria máquina. Os IPs também possuem tipos diferentes de formatos como o: IPv4 (Protocol Version 4): usa endereços de 32 bits (123.456.7.89); IPv6 (Protocol Version 6): usa endereços de 128 bits (alfanuméricos, 1234:db7:ff00:32:8764). Esses tipos possuem diferenças quanto a sua capacidade, formato, segurança (IPsec embutido em IPv6 e opcional em IPv4), configuração (IPv6 possui autoconfiguração automática) e cabeçalho (IPv6 mais simples).

\subsection*{Portas de Entrada (Portas de Protocolos de Rede)}

Portas de redes são um meio para que um serviço funcione, direcionando aplicações e serviços para um destino receber dados de uma forma simplificada. Cada porta de entrada possui grupos diferentes, como por exemplo a porta 80/443 é usada para tráfego de web (navegador) e a porta 22 usada para conexão segura (SSH). Cada porta possui 16 bits, existem 65534 portas possíveis que cada servidor pode usar, tendo outros protocolos usados por meio de uma autoridade de atribuição de números da internet (IANA), possuindo cada porta um tipo de serviço para que não haja conflitos ou imprevistos na internet, como: 53 = DNS; 67, 68 = DHCP; 80 = HTTP; 110 = POP3; 443 = HTTPS. O TCP e UDP são os protocolos mais básicos da comunicação de computadores. TCP: apresenta latência na transferência de dados, porém garante a entrega dos dados. UDP: transmite grandes quantidades de dados, porém não garante a entrega dos dados. Ambos os protocolos necessitam de endereço IP e portas para funcionar, e devido aos seus comportamentos é recomendado utilizar o IANA, tendo algumas portas que somente funcionam com TCP ou UDP, assim como TCP e UDP.

\subsection*{Cliente-Servidor}

Cliente-servidor é um modelo de rede baseado em requisições e respostas: os clientes fazem as requisições de acesso aos serviços ou recursos e os servidores fornecem o serviço ou o recurso designado na rede, sendo necessária uma rede que permita a conexão entre os clientes e servidores.

Referências:
\begin{itemize}
    \item Como a Internet funciona? --- Aprendendo desenvolvimento web | MDN
    \item \url{https://www.youtube.com/watch?v=W-_jjmkUH9k}
    \item \url{https://www.youtube.com/watch?v=jWo792sVGBY&t=85s}
    \item \url{https://www.youtube.com/watch?v=FaTlrHq8RKM}
    \item \url{https://www.youtube.com/watch?v=Xmu-EpxcROM&t=147s}
\end{itemize}

A terceira atividade do mês de abril foi algumas revisões sobre Python.

Faça um programa que receba o raio $r$, e exiba o comprimento da circunferência $C$, a área do círculo $A_c$, a área superficial da esfera $A_s$ e o volume da esfera $V$.
\[
C = 2\pi r \qquad A_c = \pi r^2 \qquad V = \frac{4}{3}\pi r^3 \qquad A_s = 4\pi r^2
\]

\begin{verbatim}
raio = float(input('informe seu raio\n'))
pi = 3.1415
c = 2*pi*raio
v = (4/3)*pi*raio**3
ac = pi*raio**2
ass = 4*pi*raio**2
print(f"comprimeto: {c}\nvolume: {v}\narea do circulo: {ac}\narea superficial: {ass}")
\end{verbatim}

Na primeira semana de maio, a atividade foi configurar o ambiente de desenvolvimento:
\begin{itemize}
    \item Instalar o Python 3 (se necessário) e o VS Code com a extensão Python.
    \item Criar um ambiente virtual (venv): uma ``caixa isolada'' que guarda as bibliotecas do projeto sem afetar o resto do computador:
    \begin{itemize}
        \item Criar: \verb|python -m venv venv|
        \item Ativar (Linux/Mac): \verb|source venv/bin/activate|
        \item Ativar (Windows): \verb|venv\Scripts\activate|
    \end{itemize}
    \item Entender o pip (gerenciador de pacotes) e criar um \verb|requirements.txt|.
\end{itemize}

Nessa atividade, eu instalei uma venv no VS Code, com a versão 3.14.4 do Python. Aprendi também que para ativar o meu ambiente virtual eu utilizo o comando \verb|.\venv\Scripts\activate|, e para desativar utiliza-se o comando \verb|deactivate|.


%\input{secao2}
\section{Relatório do mês de maio}

Neste mês fizemos os primeiros testes com a biblioteca \verb|socket|, e já começamos a estruturar o chat com o modelo cliente e servidor.

\subsection*{Parte do cliente}

\begin{verbatim}
import socket

HEADER = 64
PORT = 5050
FORMAT = 'utf-8'
DISCONNECT_MESSAGE = "!DISCONNECT"
SERVER = socket.gethostbyname(socket.gethostname())
ADDR = (SERVER, PORT)

client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
client.connect(ADDR)

def send(msg):
    message = msg.encode(FORMAT)
    msg_length = len(message)
    send_length = str(msg_length).encode(FORMAT)
    send_length += b' ' * (HEADER - len(send_length))
    client.send(send_length)
    client.send(message)
    print(client.recv(2048).decode(FORMAT))

connected = True

while connected:
    msg = input()
    send(msg)

    if msg == DISCONNECT_MESSAGE:
        connected = False

client.close()
\end{verbatim}

\subsection*{Parte do servidor}

\begin{verbatim}
import socket
import threading

HEADER = 64
PORT = 5050
SERVER = socket.gethostbyname(socket.gethostname())
ADDR = (SERVER, PORT)
FORMAT = 'utf-8'
DISCONNECT_MESSAGE = "!DISCONNECT"

server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
server.bind(ADDR)

def handle_client(conn, addr):
    print(f"[NEW CONNECTION] {addr} connected.")

    connected = True
    while connected:
        msg_length = conn.recv(HEADER).decode(FORMAT)
        if msg_length:
            msg_length = int(msg_length)
            msg = conn.recv(msg_length).decode(FORMAT)
            if msg == DISCONNECT_MESSAGE:
                connected = False

            print(f"[{addr}] {msg}")
            conn.send("Message received".encode(FORMAT))

    conn.close()

def start():
    server.listen()
    print(f"[LISTENING] Server is listening on {SERVER}")
    while True:
        conn, addr = server.accept()
        thread = threading.Thread(target=handle_client, args=(conn, addr))
        thread.start()
        print(f"[ACTIVE CONNECTIONS] {threading.active_count() - 1}")

print("[STARTING] server is starting...")
start()
\end{verbatim}

\subsection*{Explicação do código}

Este código implementa um servidor TCP concorrente utilizando as bibliotecas \verb|socket| e \verb|threading|. O objetivo do programa é permitir que vários clientes se conectem simultaneamente, criando uma thread para cada conexão.

\textbf{Bibliotecas usadas:}
\begin{itemize}
    \item \verb|import socket| --- biblioteca responsável pela comunicação em rede. Permite criar servidores, criar clientes, enviar e receber mensagens.
    \item \verb|import threading| --- biblioteca responsável por criar \emph{threads}, que permitem executar várias tarefas ao mesmo tempo. No projeto do chat: 1 cliente corresponde a 1 thread, e vários clientes a várias threads.
\end{itemize}

\textbf{Configurações iniciais:}
\begin{itemize}
    \item \verb|HEADER = 64| --- define que os primeiros 64 bytes enviados conterão o tamanho da mensagem (ex.: mensagem ``Olá'' tem tamanho 3).
    \item \verb|PORT = 5050| --- porta utilizada pelo servidor.
    \item \verb|SERVER = socket.gethostbyname(socket.gethostname())| --- obtém automaticamente o endereço IP da máquina (ex.: 192.168.1.10).
    \item \verb|ADDR = (SERVER, PORT)| --- agrupa IP e porta (ex.: ("192.168.1.10", 5050)).
    \item \verb|FORMAT = 'utf-8'| --- define a codificação utilizada, convertendo texto em bytes.
    \item \verb|DISCONNECT_MESSAGE = "!DISCONNECT"| --- mensagem utilizada para encerrar a conexão.
\end{itemize}

\textbf{Criação do servidor:} \verb|socket.socket(AF_INET, SOCK_STREAM)| cria um socket TCP, em que \verb|AF_INET| indica IPv4 e \verb|SOCK_STREAM| indica TCP. O TCP garante ordem, entrega e confiabilidade. Em seguida, \verb|server.bind(ADDR)| associa o servidor ao endereço (IP + porta).

\textbf{Função handle\_client():} executada por cada thread, recebe \verb|conn| (conexão com o cliente) e \verb|addr| (endereço do cliente). Cada cliente conectado executa uma instância dessa função. O loop \verb|while connected| mantém a comunicação ativa: recebe os primeiros 64 bytes que indicam o tamanho da mensagem, converte para número, recebe a mensagem real e a exibe. Se a mensagem for \verb|!DISCONNECT|, encerra a comunicação. A cada mensagem o servidor envia a confirmação ``Message received'' e, ao final, fecha a conexão com \verb|conn.close()|.

\textbf{Função start():} responsável por iniciar o servidor. \verb|server.listen()| coloca o servidor em modo de espera e o loop \verb|while True| o mantém funcionando continuamente. \verb|server.accept()| espera um cliente conectar e retorna a conexão e o endereço.

\textbf{Uso do threading:} para cada conexão, \verb|threading.Thread(target=handle_client, args=(conn, addr))| cria uma thread, em que \verb|target| é a função executada e \verb|args| são os argumentos enviados (equivale a \verb|handle_client(conn, addr)|). \verb|thread.start()| inicia a thread, atendendo o cliente em paralelo. \verb|threading.active_count() - 1| conta quantas threads estão ativas, subtraindo 1 para ignorar a thread principal.

\textbf{Fluxo geral:}
\begin{verbatim}
Servidor inicia
  -> listen()
  -> Cliente conecta
  -> accept()
  -> Cria thread
  -> start()
  -> handle_client()
  -> Recebe mensagens
  -> Responde
  -> Cliente desconecta
  -> Fecha conexão
\end{verbatim}

Este código implementa, portanto, um servidor concorrente usando Python, Socket e Threading.

\subsection*{A biblioteca threading}

A biblioteca \verb|threading| do Python funciona como um substituto à ideia original de projeto, que seria a criação de um servidor central. Essa biblioteca permite a execução de múltiplas tarefas simultaneamente dentro do mesmo programa por meio de threads (linhas de execução). Ela é muito utilizada em aplicações que precisam realizar várias operações ao mesmo tempo, como servidores, comunicação em rede, leitura de arquivos e espera por entrada do usuário.

No código, o threading foi usado para permitir que o servidor atendesse vários clientes ao mesmo tempo. Sempre que um cliente se conecta, o servidor cria uma nova thread responsável por tratar exclusivamente aquela conexão.

Principais funções utilizadas no código:
\begin{itemize}
    \item \verb|threading.Thread()| --- cria uma nova thread. Exemplo: \verb|thread = threading.Thread(target=handle_client, args=(conn, addr))|.
    \item \verb|target| --- define qual função será executada pela thread (ex.: \verb|target=handle_client| faz a thread executar \verb|handle_client()|).
    \item \verb|args| --- envia argumentos para a função executada pela thread (ex.: \verb|args=(conn, addr)| equivale a executar \verb|handle_client(conn, addr)|).
    \item \verb|start()| --- inicia a execução da thread (ex.: \verb|thread.start()|). Sem essa função, a thread é criada mas não executa.
    \item \verb|threading.active_count()| --- retorna o número de threads ativas no programa (ex.: \verb|threading.active_count() - 1|). O \verb|-1| desconsidera a thread principal e mostra apenas as conexões dos clientes.
\end{itemize}

\section{Relatório do mês de junho}

Nesse mês foram feitas algumas alterações no chat.

\subsection*{Nova versão do cliente}

\begin{verbatim}
import socket
import threading
import time

nickname = input("Choose a nickname: ")

HEADER = 64
PORT = 5050
FORMAT = 'utf-8'
DISCONNECT_MESSAGE = "!DISCONNECT"
SERVER = "191.52.106.190"
ADDR = (SERVER, PORT)

client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
client.connect(ADDR)

def receive():
    while True:
        try:
            message = client.recv(1024).decode(FORMAT)
            if message == 'NICK':
                client.send(nickname.encode(FORMAT))
                pass
            else:
                print(message)
        except:
            print("An error occurred")
            client.close()
            break

def send():
    connected = True

    while connected:
        
        time.sleep(0.8)
        user_input = input("> ")
        
        if user_input == DISCONNECT_MESSAGE:
            print("Disconnecting from the server...")
            connected = False
            msg = DISCONNECT_MESSAGE
            message = msg.encode(FORMAT)
            msg_length = str(len(message)).encode(FORMAT)
            msg_length += b' ' * (HEADER - len(msg_length))
            client.send(msg_length)
            client.send(message)
            time.sleep(1)

            client.close()
            break
        
        else:
            msg = f"{nickname}: {user_input}"
            message = msg.encode(FORMAT)
            msg_length = str(len(message)).encode(FORMAT)
            msg_length += b' ' * (HEADER - len(msg_length))
            client.send(msg_length)
            client.send(message)
 

receive_thread = threading.Thread(target=receive)
receive_thread.start()

send_thread = threading.Thread(target=send)
send_thread.start()
\end{verbatim}

\subsection*{Nova versão do servidor}

\begin{verbatim}
import socket
import threading

HEADER = 64
PORT = 5050
SERVER = "0.0.0.0"
ADDR = (SERVER, PORT)
FORMAT = "utf-8"
DISCONNECT_MESSAGE = "!DISCONNECT"

server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
server.bind(ADDR)
server.listen()

clients = []
nicknames = []

def remove_client(client):
    index = clients.index(client)
    client.close()
    print(clients)
    nickname = nicknames[index]
    clients.remove(client)
    print(f"{nickname} desconectou do chat")
    broadcast(f"{nickname} left the chat".encode(FORMAT))
    nicknames.remove(nickname)

def broadcast(message):
    for client in clients:
        client.send(message)

def handle_client(client, addr):
    connected = True
    while connected:
        try:
            msg_length = client.recv(HEADER).decode(FORMAT)
            if msg_length:
                msg_length = int(msg_length)
                msg = client.recv(msg_length).decode(FORMAT)
                if msg == DISCONNECT_MESSAGE:
                    remove_client(client)
                    break
                broadcast(msg.encode(FORMAT))

        except:
            remove_client(client)
            break
    client.close()

def receive():
    while True:
        client, addr = server.accept()
        print(f"Connected with {str(addr)}")

        client.send("NICK".encode(FORMAT))
        nickname = client.recv(1024).decode(FORMAT)
        nicknames.append(nickname)
        clients.append(client)

        print(f"Nickname of client is {nickname}")
        broadcast(f"{nickname} joined the chat".encode(FORMAT))
        client.send("Connected to the server".encode(FORMAT))

        thread = threading.Thread(target=handle_client, args=(client, addr))
        thread.start() 

print("[STARTING] server is listening...")
receive()
\end{verbatim}

As principais atualizações feitas são:

\begin{itemize}
    \item \textbf{Na parte do cliente:} Ao iniciar o código, o programa solicita que o usuário informe seu nome (nickname), que será utilizado para identificá-lo no chat. O cliente passou a enviar e receber mensagens simultaneamente por meio de duas threads: uma responsável pelo envio e outra pelo recebimento das mensagens. Além disso, ele passa a receber mensagens enviadas por qualquer cliente conectado ao servidor, permitindo uma comunicação em grupo.

    \item \textbf{Na parte do servidor:} O servidor passou a manter uma lista dos clientes conectados e seus respectivos nicknames, permitindo identificar o remetente de cada mensagem. Quando um novo cliente entra no chat, todos os participantes recebem uma mensagem informando sua conexão; da mesma forma, quando um cliente se desconecta, todos são notificados. Outra mudança importante é a utilização do endereço \verb|0.0.0.0|, permitindo que o servidor aceite conexões por qualquer interface de rede.
\end{itemize}

Novas funções:

\begin{itemize}
    \item \verb|receive()|: executada em uma thread separada, fica constantemente aguardando e exibindo as mensagens recebidas do servidor, permitindo que o usuário continue digitando enquanto recebe novas mensagens.

    \item \verb|broadcast()|: envia uma mesma mensagem para todos os clientes conectados ao servidor, tornando possível o funcionamento do chat em grupo.

    \item \verb|remove_client()|: identifica o cliente que se desconectou, fecha sua conexão, remove-o das listas de clientes e nicknames e informa aos demais participantes que ele saiu do chat.
\end{itemize}
